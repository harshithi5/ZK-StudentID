node_modules/
dist/
.env
*.log

# Circuit build artifacts (large binary files — regenerated via `npm run setup`)
circuits/build/
circuits/circom.exe

# Frontend artifacts copy (regenerated by setup)
frontend/public/artifacts/

# Backend artifacts are kept (needed to run without recompiling)
# but zkey is large — can be regenerated
# backend/artifacts/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
